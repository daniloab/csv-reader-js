{
  "name": "csv-reader-js",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.14.38"
  },
  "scripts": {
    "es": "esbuild ./src/reader.ts --bundle --platform=node --outdir=dist",
    "read": "yarn es && node dist/reader.js"
  },
  "dependencies": {
    "@fast-csv/format": "^4.3.5",
    "@fast-csv/parse": "^4.3.6",
    "fast-csv": "^4.3.6"
  }
}
